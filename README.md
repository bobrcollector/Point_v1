# Point v1

Мобильное приложение для поиска и организации событий на базе .NET MAUI.

## 🚀 Быстрый старт

### Требования

- **.NET 10.0 SDK** или выше
- **Visual Studio 2022** (17.8+) или **Visual Studio Code** с расширениями для .NET MAUI
- **Android SDK** (для Android разработки)
- **Xcode** (для iOS/macOS разработки, только на macOS)
- **Windows SDK** (для Windows разработки)

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd Point_v1
```

2. Восстановите зависимости:
```bash
dotnet restore
```

3. Настройте Firebase:
   - Убедитесь, что файл `google-services.json` находится в корне проекта
   - Проверьте, что API ключи в `Services/FirebaseRestService.cs` актуальны

4. Запустите приложение:

**Android:**
```bash
dotnet build -t:Run -f net10.0-android
```

**Windows:**
```bash
dotnet build -t:Run -f net10.0-windows10.0.19041.0
```

**iOS (только на macOS):**
```bash
dotnet build -t:Run -f net10.0-ios
```

**macOS:**
```bash
dotnet build -t:Run -f net10.0-maccatalyst
```

### Запуск через Visual Studio

1. Откройте `Point_v1.slnx` в Visual Studio 2022
2. Выберите целевую платформу (Android, iOS, Windows, macOS)
3. Нажмите F5 или кнопку "Запустить"

## 📱 Поддерживаемые платформы

- ✅ Android (API 21+)
- ✅ iOS (15.0+)
- ✅ Windows (10.0.17763.0+)
- ✅ macOS (15.0+)

## 🛠 Технологии

- **.NET MAUI** - кроссплатформенный фреймворк
- **Firebase Realtime Database** - база данных
- **Firebase Authentication** - аутентификация
- **Firebase Storage** - хранение файлов
- **Yandex Maps API** - карты и геокодирование
- **MVVM** - архитектурный паттерн
- **Dependency Injection** - внедрение зависимостей

## 📚 Документация

Подробная документация доступна в файле [DOCUMENTATION.md](DOCUMENTATION.md), включающая:
- Архитектуру приложения
- Схему базы данных
- Примеры использования API
- Структуру проекта
- Используемые технологии

## 🔄 CI/CD для Android

Проект использует GitHub Actions для автоматической сборки Android релизных версий.

### Создание релиза

**Через тег:**
```bash
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0
```

После отправки тега автоматически:
- ✅ Соберется Android APK
- ✅ Создастся GitHub Release с APK файлом

**Подробная инструкция:** См. [CI_CD_ANDROID_SETUP.md](CI_CD_ANDROID_SETUP.md)

### Workflows

- `release-android.yml` - Релизные сборки для Android
- `build-android.yml` - Проверка кода при каждом push

## 🔧 Настройка окружения

### Android

1. Установите Android SDK через Visual Studio Installer
2. Настройте эмулятор или подключите физическое устройство
3. Включите режим разработчика на устройстве

### iOS

1. Установите Xcode из App Store
2. Настройте Apple Developer аккаунт
3. Подпишите приложение в настройках проекта

### Windows

1. Установите Windows SDK
2. Включите режим разработчика в настройках Windows

## 📦 Зависимости

Основные NuGet пакеты:
- `CommunityToolkit.Maui` (12.2.0)
- `Newtonsoft.Json` (13.0.3)
- `Microsoft.Extensions.Logging.Debug` (10.0.0-rc.1.25451.107)

## 🐛 Отладка

Для отладки используйте:
- **Visual Studio Debugger** - встроенный отладчик
- **Output Window** - логи приложения
- **Firebase Console** - мониторинг базы данных

## 👥 Автор
Блашкина Екатерина группа 413-ИС-22

