# CI/CD настройка для Point v1 (Android)

## Обзор

Этот проект использует GitHub Actions для автоматической сборки Android версий приложения.

## Workflows

### 1. `build-android.yml` - Проверка кода

**Когда запускается:**
- При каждом push в ветки `main` или `develop`
- При создании Pull Request

**Что делает:**
- Собирает Android версию (Debug)
- Проверяет, что код компилируется

**Цель:** Проверка, что код компилируется без ошибок

### 2. `release-android.yml` - Релизные сборки

**Когда запускается:**
- При создании тега `v*` (например: `v1.0.0`)
- Вручную через GitHub Actions UI

**Что делает:**
- Собирает Release версию для Android (APK)
- Создает GitHub Release с APK файлом

**Цель:** Создание готовой к распространению версии приложения

## Как создать релиз

### Вариант 1: Через тег (рекомендуется)

```bash
# 1. Обновите версию в Point_v1.csproj (опционально)
# ApplicationDisplayVersion = "1.0.0"

# 2. Создайте тег
git tag -a v1.0.0 -m "Release version 1.0.0"

# 3. Отправьте тег на GitHub
git push origin v1.0.0
```

После отправки тега GitHub Actions автоматически запустит сборку релиза.

### Вариант 2: Вручную через GitHub

1. Перейдите в раздел **Actions** в репозитории
2. Выберите workflow **Android Release Build**
3. Нажмите **Run workflow**
4. Укажите версию (например: `1.0.0`)
5. Нажмите **Run workflow**

## Результаты сборки

### После успешной сборки

1. **Артефакты** доступны в разделе Actions:
   - `android-apk-release` - Android APK файл

2. **GitHub Release** создается автоматически:
   - Перейдите в раздел **Releases**
   - Найдите версию вашего релиза
   - Скачайте APK файл

## Настройка Android подписи (опционально)

Для подписи Android APK нужно:

1. Создать keystore:
```bash
keytool -genkey -v -keystore point-release.keystore \
  -alias point-key -keyalg RSA -keysize 2048 -validity 10000
```

2. Добавить секреты в GitHub:
   - Settings → Secrets and variables → Actions
   - Добавьте:
     - `ANDROID_KEYSTORE_PASSWORD` - пароль keystore
     - `ANDROID_KEY_ALIAS` - алиас ключа (point-key)
     - `ANDROID_KEY_PASSWORD` - пароль ключа
     - `ANDROID_KEYSTORE` - содержимое keystore файла (base64)

3. Обновите workflow для использования подписи:
   - Измените `AndroidKeyStore=false` на `true`
   - Добавьте пути к keystore

## Отслеживание сборок

### Проверить статус сборки:

1. Перейдите в раздел **Actions**
2. Выберите нужный workflow
3. Посмотрите статус каждого job:
   - ✅ Зеленая галочка - успешно
   - ❌ Красный крестик - ошибка
   - 🟡 Желтый круг - в процессе

### Логи сборки:

- Нажмите на job (например, "Build Android Release APK")
- Разверните нужный step
- Просмотрите логи для диагностики

## Решение проблем

### Ошибка сборки Android

**Проблема:** Ошибки компиляции
**Решение:** 
- Проверьте логи в GitHub Actions
- Убедитесь, что все зависимости восстановлены
- Проверьте версию .NET SDK

### Артефакты не найдены

**Проблема:** APK файлы не созданы
**Решение:**
- Проверьте путь к файлам в логах
- Убедитесь, что сборка прошла успешно
- Проверьте параметры publish команды

## Дополнительная информация

- [GitHub Actions документация](https://docs.github.com/en/actions)
- [.NET MAUI документация](https://learn.microsoft.com/en-us/dotnet/maui/)
- [Android подпись приложений](https://developer.android.com/studio/publish/app-signing)

