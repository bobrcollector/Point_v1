<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="Point_v1.Views.HomePage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Лента событий"
             x:Name="Page">
    <ScrollView>
    <Grid RowDefinitions="Auto,*,Auto">

        <VerticalStackLayout Grid.Row="0" Spacing="10" Padding="15,10">
            <Label Text="Лента событий" 
                   FontSize="20" 
                   FontAttributes="Bold"
                   HorizontalOptions="Center"/>
        </VerticalStackLayout>

 
        <StackLayout Grid.Row="1" Padding="15" Spacing="10" BindableLayout.ItemsSource="{Binding Events}">

            <BindableLayout.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="15">
                    <Label Text="🎉" FontSize="40" HorizontalOptions="Center"/>
                    <Label Text="Событий пока нет" 
                           FontSize="18"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"/>
                    <Label Text="Создайте первое событие!" 
                           FontSize="14"
                           TextColor="Gray"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </BindableLayout.EmptyView>

            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="0,5" Padding="15" CornerRadius="15" HasShadow="True">
                        <VerticalStackLayout Spacing="10">
                            <Label Text="{Binding Title}" 
                                   FontSize="18" 
                                   FontAttributes="Bold"/>

                            <Label Text="{Binding ShortDescription}" 
                                   FontSize="14"/>

                            <Grid ColumnDefinitions="*,*">
                                <Label Grid.Column="0"
                                       Text="{Binding Address}" 
                                       FontSize="12"
                                       TextColor="Gray"/>

                                <Label Grid.Column="1"
                                       Text="{Binding DateDisplay}" 
                                       FontSize="12"
                                       TextColor="Gray"
                                       HorizontalOptions="End"/>
                            </Grid>

                            <Button Text="Я пойду!"
                                    BackgroundColor="#512BD4"
                                    Padding="15,8"
                                    CornerRadius="8"
                                    FontSize="12"/>
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </BindableLayout.ItemTemplate>

        </StackLayout>


        <Frame Grid.Row="2"
               IsVisible="{Binding IsGuestMode}"
               BackgroundColor="#DFD8F7"
               Padding="15"
               Margin="10"
               CornerRadius="10"
               HasShadow="True">
            <Grid ColumnDefinitions="*,Auto">
                <Label Grid.Column="0"
                       Text="Войдите, чтобы создавать события"
                       VerticalOptions="Center"/>
                <Button Grid.Column="1"
                        Text="Войти"
                        Command="{Binding GoToLoginCommand}"
                        BackgroundColor="#512BD4"
                        Padding="15,8"/>
            </Grid>
        </Frame>


        <Button Grid.Row="1"
                Text="+" 
                Command="{Binding CreateEventCommand}"
                FontSize="24"
                FontAttributes="Bold"
                WidthRequest="60"
                HeightRequest="60"
                CornerRadius="30"
                BackgroundColor="#512BD4"
                HorizontalOptions="End"
                VerticalOptions="End"
                Margin="0,0,20,20"/>

    </Grid>
    </ScrollView>
</ContentPage>